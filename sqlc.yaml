version: "2"
sql:
  - engine: "postgresql"
    schema: "sqls/schema.sql"
    queries: "sqls/queries"
    gen:
      go:
        package: "db"
        out: "src/lib/db"
        emit_json_tags: true
        emit_prepared_queries: false
