version: "2"
sql:
  - engine: "postgresql"
    schema: "backend/sqls/init/schema.sql"
    queries: "backend/sqls/queries"
    gen:
      go:
        package: "db"
        out: "backend/internal/db"
        emit_json_tags: true
        emit_prepared_queries: false
