syntax = "proto3";

package session;

option go_package = "proto/session/";

message SessionRequest {
  string listen_path = 1;
}

message SessionResponse {}

service Session {
  rpc SetSession(SessionRequest) returns (SessionResponse);
}
