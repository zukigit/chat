version: "2"
sql:
  - engine: "postgresql"
    schema: "sqls/init/schema.sql"
    queries: "sqls/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_prepared_queries: false
