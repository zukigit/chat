meta {
  name: signup_user2
  type: http
  seq: 3
}

post {
  url: {{GATEWAY_URL}}/signup
  body: json
  auth: inherit
}

body:json {
  {
    "type": "email",
    "username": "{{USER_NAME_USER2}}",
    "passwd": "{{USER_PASSWD_USER2}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
