meta {
  name: friends_request_user2
  type: http
  seq: 10
}

post {
  url: {{GATEWAY_URL}}/friends/request
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{TOKEN_USER2}}
}

body:json {
  {
    "username": "{{USER_NAME_USER1}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
